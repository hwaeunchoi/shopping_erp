# SRS/UI와이어프레임 대비 구현 현황 대조

**작성일**: 2026-07-02
**대상 문서**: `SRS_쇼핑몰통합ERP.md` (v1.0), `UI와이어프레임_v1.1_추가반영.md`
**대상 코드**: 1~8단계(백엔드+프론트엔드 MVP) + 품질/운영 안정성 1~7순위 완료 시점

이 문서는 코드를 변경하지 않는 **읽기 전용 점검 결과**이다. 발견된 차이/공백은
바로 구현하지 않았다 - "품질/운영 안정성 우선, 신규 기능 추가는 후순위"라는
현재 작업 방침에 따라, 실제 구현 여부는 이 문서를 본 뒤 사용자가 결정한다.

## 결론 요약

- **DB 스키마(models/)는 SRS + UI 와이어프레임 v1.1 addendum의 요구사항을
  이미 100% 반영해두었다** - v1.1에서 추가 요구한 4개 테이블
  (`recent_views`, `favorites`, `task_execution_history`,
  `integration_status`)까지 전부 존재한다(총 50개 테이블).
- **Repository 계층도 스키마의 대부분을 커버한다** - 배송/교환/반품/취소,
  상품 성과 요약, KPI 목표치까지 Repository 클래스가 이미 만들어져 있다.
- **Service/API/프론트엔드 계층은 그 중 핵심 흐름(주문 수집 → 정산 →
  비용 → 일별 손익 → 최소 대시보드)만 구현했다.** SRS의 기능요구사항
  중 상당수, UI 와이어프레임 v1.1의 추가 요소는 전부 아직 화면/API로
  연결되지 않은 상태다. 이는 설계 누락이 아니라 "1~8단계는 MVP 범위"였기
  때문으로 보인다.
- 딱 하나, **설계와 실제 구현이 어긋난 부분**을 발견했다(우선순위/설정
  누락이 아니라 진짜 불일치): 손익 계산이 `platform_fee_rules` 테이블
  대신 서비스 코드에 하드코딩된 수수료율을 사용한다. **(2026-07-02 수정
  완료 - 아래 2절 참고)**

---

## 1. SRS 기능요구사항(FR) 대조

범례: ✅ 구현됨 / 🟡 부분 구현·설계와 다름 / ❌ 미구현

### 3.1 쇼핑몰 연동
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-MALL-01 5개 쇼핑몰 수집 | ✅ | 5개 더미 커넥터 전부 존재 |
| FR-MALL-02 공통 인터페이스 플러그인 구조 | ✅ | `base_mall_connector.py` 상속 구조 |
| FR-MALL-03 8종 주문상태 | 🟡 | 코드상 상태값 정의는 있으나 SRS의 8종(신규주문~환불) 문자열과 1:1 매핑되는지는 이번 점검에서 확인하지 않음 |
| FR-MALL-04 UPSERT 중복방지 | ✅ | `OrderSyncService`, 1순위 테스트로 검증됨(dedup 테스트) |
| FR-MALL-05 상태변경 이력 기록 | ✅ | `OrderStatusHistory`에 실제로 기록함(`order_sync_service.py`) |
| FR-MALL-06 인증정보 `.env`/암호화 관리 | ✅ | `.env` + `ApiCredential`(Fernet 암호화 설계) |
| FR-MALL-07 재시도 로직 + 로그 + 화면 알림 | ❌ | 더미 커넥터에 재시도 로직 없음, 실패 시 `Notification` 레코드를 생성하는 코드 없음 |

### 3.2 광고 플랫폼 연동
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-AD-01 1차 범위(네이버 검색/쇼핑광고, 쿠팡광고) | ✅ | 3개 더미 커넥터 존재. Meta/Google/카카오는 SRS도 "향후"로 명시 - 미구현이 정상 |
| FR-AD-02 공통 인터페이스 | ✅ | `base_ad_connector.py` |
| FR-AD-03 수집 항목(노출/클릭/CPC 등) | 🟡 | `AdPerformanceDaily` 컬럼 존재, ROAS/CPC/CPM이 계산되어 노출되는 API는 없음(원시 수치만 조회 가능) |
| FR-AD-04 캠페인-상품 연결 | 🟡 | `AdCampaign.product_option_id` 컬럼은 있으나 이를 이용해 상품별 광고 기여도를 분석하는 서비스/API 없음 |
| FR-AD-05 일 단위 중복방지 수집 | ✅ | `ad_collect_job` |

### 3.3 주문/배송/교환/반품/취소
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-ORD-01 플랫폼/상태/기간별 조회 | 🟡 | `GET /api/orders`는 상태(status_filter)만 지원, 플랫폼/기간 필터 없음(기존 주문 목록 화면은 이번 작업 범위 밖) |
| FR-ORD-02 미배송 대시보드 강조 | 🟡 **(2026-07-02 배송관리 화면 구현 완료, 대시보드 강조는 미구현)** | `GET /api/shipments`로 배송 등록/조회/상태변경 가능해짐. 다만 대시보드에서 "미배송 N건" 경고를 표시하는 기능은 아직 없음(3.8 FR-DASH-01과 연동 필요) |
| FR-ORD-03 교환/반품/취소 관리 | ✅ **(2026-07-02 구현 완료)** | `ExchangeService`/`ReturnService`/`CancellationService` + `/api/exchanges`, `/api/returns`, `/api/cancellations` + 프론트엔드 3개 화면. 등록/조회/상태변경/검색/필터/페이지네이션 전부 구현, 완료 시 `orders.status` 자동 동기화(교환=EXCHANGED, 반품=REFUNDED, 취소=CANCELED) + 반품 입고 시 재고 복원, 취소 시 예약재고 해제까지 연동됨 |
| FR-ORD-04 교환율/반품율/취소율 자동계산 | ❌ | 이번 작업 범위 밖(요청받은 범위는 등록/조회/상태변경까지). 개별 건수는 `GET /api/exchanges`, `/returns`, `/cancellations`의 `total` 필드로 셀 수 있으나, 비율(전체 주문 대비 %) 계산/노출 로직은 없음 |
| FR-ORD-05 주문 엑셀 내보내기 | ❌ | `openpyxl`이 requirements.txt에는 있으나 실제 사용하는 코드 없음 |

### 3.4 상품 및 원가 관리
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-PRD-01 상품 마스터 관리 | 🟡 | 조회만 가능(`products.py`에 "쓰기는 아직 대상 아님"이라고 코드 주석에도 명시됨) |
| FR-PRD-02 원가 이력 관리 | ❌ | `ProductCostHistory` 테이블은 있으나 Repository/Service/API 전무 |
| FR-PRD-03 플랫폼별 상품코드 매핑 | ✅ | `ProductPlatformMap` 테이블 존재, 시딩 데이터에도 반영(테스트 fixture) |
| FR-PRD-04 베스트/워스트 상품 순위 | ❌ | `ProductPerformanceSummaryRepository`는 있으나 이를 채우는 계산 로직과 API 없음 |

### 3.5 비용 관리
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-COST-01 비용 항목별 등록/관리 | ✅ | `costs.py` GET/POST |
| FR-COST-02 고정비/변동비, 플랫폼/상품별 구분 | 🟡 | 컬럼(category, platform_id, product_option_id)은 있으나 고정비/변동비 구분 필드나 집계 로직은 없음 |
| FR-COST-03 플랫폼 수수료율 자동 계산 | ✅ **(2026-07-02 수정 완료)** | `profit_calculation_service.py`가 `PlatformFeeRuleRepository`를 통해 `platform_fee_rules`를 조회하도록 변경(2절 참고) |

### 3.6 매출 및 손익 관리
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-PROFIT-01 상세 지표 자동계산 | 🟡 | 총매출/순매출/주문건수/원가/수수료/순이익/순이익률은 계산됨. 광고비/광고전환매출/ROAS/배송비/포장비/기타비용은 `ProfitLossSummary` 컬럼엔 있지만 `calculate_daily()`가 채우는지 이번엔 코드를 전부 추적하지 않음(요약 함수 내부에 `_sum_ad_performance`/`_sum_costs` 호출이 보여 일부는 채워지는 것으로 보임 - 정밀 검증 필요) |
| FR-PROFIT-02 일/주/월/플랫폼/상품 단위 조회 | 🟡 | `calculate_daily`/`calculate_daily_range`만 존재. 주/월/플랫폼/상품 단위 계산 로직 없음(조회 API의 `period_type` 파라미터는 있지만 이를 채우는 계산기가 일 단위뿐) |
| FR-PROFIT-03 취소/반품/환불 자동 차감 | ❌ **(2026-07-02 재확인: 여전히 미구현)** | 3.3 교환/반품/취소 관리는 구현되어 `orders.status`가 CANCELED/REFUNDED/EXCHANGED로 정확히 바뀌지만, `profit_calculation_service.py`의 `calculate_daily()`는 `order_date` 기준으로 해당 날짜의 주문을 전부 집계할 뿐 `status`로 걸러내지 않는다. 즉 취소/반품된 주문도 매출에 그대로 잡힌다 - 다음 우선순위(매출/손익) 작업에서 처리 필요 |

### 3.7 보고서
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-REPORT-01~04 일/주/월별 보고서, 엑셀/PDF 다운로드 | ❌ | `reports/`, `reports/generated/`, `reports/templates/` 디렉터리와 `openpyxl`/`reportlab` 의존성만 준비되어 있고 실제 생성 코드 없음 |

### 3.8 대시보드
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-DASH-01 오늘/이번달 요약, 미배송/교환/반품 경고 | 🟡 | `DashboardPage.tsx`가 최근 7일 순매출/순이익/주문수 요약 테이블만 표시. "오늘"/"이번 달" 구분, 미배송/교환대기/반품대기 경고는 없음(3.3 미구현과 연동) |
| FR-DASH-02 5종 그래프 | ❌ | 현재 화면은 표(table)뿐, 그래프 라이브러리(plotly 등) 미사용 |
| FR-DASH-03 카드/그래프 클릭 시 drill-down | ❌ | 미구현 |

### 3.9 사용자 및 권한 관리
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-USER-01 로그인 + bcrypt 해시 | ✅ | `passlib[bcrypt]` |
| FR-USER-02 Role 2단계 이상 | ✅ | Admin/Manager/Viewer 3단계 |
| FR-USER-03 메뉴별 권한 매핑 구조 | ✅ | `Permission`/`RolePermission` + `require_permission()` |
| FR-USER-04 로그인/활동 로그 | 🟡 | `AuditLog` 테이블은 있으나 실제로 로그를 남기는 코드 없음(로그인 성공/실패는 Python 로그 파일에는 남지만 DB 테이블엔 기록 안 됨) |

### 3.10 로그 및 백업
| 항목 | 상태 | 비고 |
|---|---|---|
| FR-LOG-01 API 수집이력/사용자활동/오류 로그 | 🟡 | 날짜별 로그 파일(`config/logging_config.py`)은 있음. `SystemLog` DB 테이블에 API 수집 이력을 남기는 코드는 확인 안 됨 |
| FR-LOG-02 자동 백업 | ✅ | `backup_job.py`, 매일 03:00 스케줄 |
| FR-LOG-03 백업 보관정책(기간/개수, 자동정리) | ✅ | `_apply_retention()`이 `backup_retention_days`/`backup_max_count` 둘 다 실제로 적용함 |

---

## 2. 설계-구현 불일치 - 수정 완료 (2026-07-02)

> **처리 결과: 수정 완료.** 아래는 발견 당시 기록이며, 실제 조치 내용은
> README.md "23. 수수료율 하드코딩 제거" 절을 참고.

**`services/profit_calculation_service.py`의 손익 계산이 `platform_fee_rules`
테이블을 쓰지 않고 하드코딩된 수수료율을 사용한다.**

```python
FEE_RATE_BY_PLATFORM_CODE = {
    "naver_smartstore": 0.035,
    "coupang": 0.10,
    "esm": 0.12,
    "elevenst": 0.12,
    "kakao_shopping": 0.035,
}
DEFAULT_FEE_RATE = 0.10
```

DB에는 정확히 이 목적을 위한 `platform_fee_rules` 테이블(플랫폼별/기간별
수수료율)이 이미 설계되어 있는데, 서비스는 이를 조회하지 않는다. 즉
운영자가 화면에서(향후 "설정" 메뉴가 생겨) 수수료율을 바꿔도 손익 계산에
반영되지 않는다. 이 부분은 명백히 "아직 다 못 만든 부분"이지 의도된
설계는 아닌 것으로 보인다 - `platform_fee_rules`가 애초에 이 하드코딩을
대체하기 위해 설계된 테이블이기 때문이다.

수정 방향은 두 가지가 가능하다.
1. `PlatformFeeRuleRepository`를 만들고 `profit_calculation_service.py`가
   플랫폼별 수수료율을 DB에서 조회하도록 변경 (하드코딩 dict는 fallback으로
   남기거나 제거)
2. 지금은 그대로 두고, 이 문서에 기록만 해둔 채 별도 작업으로 처리

**설계/스키마는 변경하지 않는다는 작업 규칙에 걸리지 않는 수정**(테이블은
이미 있고 서비스 코드만 바뀜)이라 원하시면 바로 진행 가능합니다.

---

## 3. UI 와이어프레임 v1.1 addendum - 구현 여부

v1.1이 요구한 4개 지원 테이블은 전부 존재하지만, 이를 사용하는
Repository/Service/API/프론트엔드는 **하나도 없다**.

| v1.1 요소 | DB 테이블 | Repository | API | 프론트엔드 |
|---|---|---|---|---|
| 글로벌 통합검색 | (기존 테이블 조합 조회) | - | ❌ | ❌ |
| 최근 본 항목 | `recent_views` | ❌ | ❌ | ❌ |
| 즐겨찾기 | `favorites` | ❌ | ❌ | ❌ |
| 다크모드 토글 | `users.theme_preference` (컬럼은 있음, `UserProfile` 응답에도 포함됨) | - | 🟡 조회만 가능, 저장 API 없음 | ❌ 토글 UI 없음 |
| AI Assistant 패널 | `ai_analysis_results` | ❌ | ❌ | ❌ |
| 대시보드 빠른실행 버튼 5종 | `task_execution_history` | ❌ | ❌ | ❌ |
| 시스템 모니터링(연동상태/작업이력/시스템상태) | `integration_status`, `task_execution_history`, `backup_history` | ❌(연동상태만) | ❌ | ❌ |
| Drill-down 공통 규칙 | - | - | - | ❌ |
| 반응형(태블릿) 레이아웃 | - | - | - | 미확인(이번 점검 범위 아님 - 실제 브라우저 리사이즈 테스트 필요) |

즉 v1.1 addendum은 **DB 설계 단계에서만 반영되었고, 애플리케이션 레이어는
전혀 손대지 않은 상태**다.

---

## 4. 이 문서를 보고 결정할 것

이번 점검은 읽기 전용이라 아무 코드도 바꾸지 않았다. 발견된 항목 중
실제로 작업할 대상이 있다면 별도로 지시해 주시기 바랍니다. 참고로 규모
가늠을 위한 대략적 분류:

- **~~작은 작업(품질 성격에 가까움)~~**: ~~2절의 수수료율 하드코딩 → DB 조회로
  전환~~ - 2026-07-02 완료
- **~~중간 작업 1순위~~**: ~~배송/교환/반품/취소 관리(Repository/Service/API/
  화면 전체)~~ - 2026-07-02 완료(README "24. 배송/교환/반품/취소 관리" 참고)
- **중간 작업(새 기능, 기존 Repository 재사용 가능)**: 상품 CRUD API, 주문
  필터 확장(플랫폼/기간), 취소/반품/환불 주문을 매출 집계에서 제외(FR-PROFIT-03,
  `profit_calculation_service.py` 수정 필요)
- **큰 작업(새 기능, Repository부터 신규 작성 필요)**: 보고서(엑셀/PDF)
  생성, UI v1.1 addendum 전체(검색/즐겨찾기/AI패널/시스템모니터링/
  대시보드 그래프)
