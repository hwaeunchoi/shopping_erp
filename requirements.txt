# === 웹 프레임워크 / API ===
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.32          # OAuth2PasswordRequestForm(로그인 폼) 파싱에 필요

# === DB / ORM / 마이그레이션 ===
# 2.0.35는 Python 3.14 typing 변경사항과 충돌하는 버그가 있어 2.0.51로 상향 (2.0 API는 동일)
SQLAlchemy==2.0.51
alembic==1.13.2
psycopg[binary]==3.2.1    # PostgreSQL 드라이버 (Docker 환경은 PostgreSQL을 사용하므로 필수 설치)

# === 설정 / 검증 ===
# 2.9.2(pydantic-core 2.23.4)는 Python 3.14용 wheel이 없어 상향 (빌드 도구 없이 설치 가능한 최소 상향)
pydantic==2.13.4
pydantic-settings==2.14.2

# === 인증/보안 ===
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # 비밀번호 해시
bcrypt==4.0.1                      # passlib 1.7.4는 bcrypt>=4.1과 호환 안 됨(__about__ 속성 제거) — 명시 고정
cryptography==43.0.1               # API Key 암호화(Fernet)

# === 스케줄러 / 비동기 HTTP ===
APScheduler==3.10.4
httpx==0.27.2

# === 엑셀 / PDF ===
openpyxl==3.1.5
reportlab==4.2.2

# === 데이터 처리(향후 AI/분석 모듈 대비) ===
# 2.2.2는 Python 3.14용 사전 빌드 wheel이 없어 2.3.3으로 상향 (빌드 도구 없이 설치 가능한 최소 상향)
pandas==2.3.3

# === 테스트 ===
pytest==8.3.3
pytest-cov==5.0.0

# === 코드 품질 (개발용) ===
ruff==0.15.20
black==26.5.1
isort==8.0.1
mypy==2.1.0
pre-commit==4.6.0

# === 개발 편의 ===
python-dotenv==1.0.1
