# 쇼핑몰 통합 ERP — UI 와이어프레임 추가 반영본 (v1.1)

**기준 문서**: UI와이어프레임 v1.0 (승인 완료)
**본 문서의 성격**: v1.0의 15개 화면 구조를 유지한 채, 실무 효율화 기능을 추가로 얹는 **addendum**
**작성일**: 2026-07-01

이번 요청사항 대부분은 화면 설계뿐 아니라 이를 지탱할 **작은 지원 테이블 4개**가 추가로 필요합니다. ERD를 전면 재설계하지 않고, 0장에서 추가 테이블만 짧게 정리한 뒤 화면 반영 내용을 이어갑니다.

---

## 0. ERD 추가분 (기존 41개 테이블에 4개 추가 → 총 45개)

#### recent_views (최근 본 항목)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | INTEGER | PK | |
| user_id | INTEGER | FK → users.id, NOT NULL | |
| target_type | VARCHAR(20) | NOT NULL | ORDER/PRODUCT/CUSTOMER |
| target_id | INTEGER | NOT NULL | |
| viewed_at | DATETIME | NOT NULL, INDEX | |

- 인덱스: `idx_recent_views_user (user_id, viewed_at)` — 사용자별 최근순 조회
- 오래된 기록은 배치로 정리(사용자당 최근 N건만 유지)

#### favorites (즐겨찾기)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | INTEGER | PK | |
| user_id | INTEGER | FK → users.id, NOT NULL | |
| target_type | VARCHAR(20) | NOT NULL | PRODUCT/REPORT/CUSTOMER 등 확장 가능 |
| target_id | INTEGER | NOT NULL | REPORT의 경우 보고서 유형/필터 조합을 나타내는 키 |
| created_at | DATETIME | NOT NULL | |

- UNIQUE(user_id, target_type, target_id)

#### task_execution_history (작업 실행 이력 — 주문수집/광고수집/백업/보고서생성/동기화 공통)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | INTEGER | PK | |
| task_type | VARCHAR(30) | NOT NULL | ORDER_COLLECT/AD_COLLECT/BACKUP/REPORT_GENERATE/FULL_SYNC |
| target | VARCHAR(50) | NULL | 예: platform=coupang, ad_platform=naver_search_ad |
| trigger_type | VARCHAR(10) | NOT NULL | SCHEDULE(자동)/MANUAL(수동) |
| triggered_by | INTEGER | FK → users.id, NULL | 수동 실행 시 사용자 |
| status | VARCHAR(20) | NOT NULL | RUNNING/SUCCESS/FAILED |
| started_at | DATETIME | NOT NULL, INDEX | |
| finished_at | DATETIME | NULL | |
| result_summary | TEXT | NULL | 예: "신규주문 23건 수집, 실패 0건" |
| error_message | TEXT | NULL | |

- **관계**: `system_logs`는 세부 로그 메시지 위주, 이 테이블은 "실행 단위"를 대시보드/모니터링 화면에서 한눈에 보기 위한 요약 테이블로 역할을 분리한다.
- 대시보드의 "빠른 실행 버튼" 클릭 → 이 테이블에 RUNNING 레코드 생성 → 완료 시 SUCCESS/FAILED로 갱신

#### integration_status (플랫폼/광고 연동 상태 스냅샷)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | INTEGER | PK | |
| integration_type | VARCHAR(20) | NOT NULL | MALL/AD |
| integration_code | VARCHAR(30) | NOT NULL | platforms.code 또는 ad_platform_code |
| status | VARCHAR(20) | NOT NULL | NORMAL/ERROR/TOKEN_EXPIRING |
| last_success_at | DATETIME | NULL | |
| last_error_at | DATETIME | NULL | |
| last_error_message | TEXT | NULL | |
| token_expires_at | DATETIME | NULL | api_credentials.expires_at과 연동 |
| updated_at | DATETIME | NOT NULL | |

- UNIQUE(integration_type, integration_code)
- 매 수집 작업(`task_execution_history`) 종료 시 이 테이블이 갱신되는 구조 — "연동 상태 모니터링" 화면이 이 테이블만 조회하면 되도록 하여 화면 응답속도 확보

---

## 1. 공통 레이아웃 변경사항 (상단바 재설계)

```
┌───────────────────────────────────────────────────────────────────┐
│[로고] [🔍 통합검색: 주문번호/상품명/SKU/고객명/송장번호...]        │
│                          [🕐최근본항목▾][⭐즐겨찾기▾][🔔3][🌙][👤▾][🤖]│
├───────────────────────────────────────────────────────────────────┤
```

**신규 구성요소**
- **글로벌 통합검색**: 상단 중앙 고정. 입력 시 `orders/products/product_options/customers/shipments` 를 동시 질의하여 드롭다운에 카테고리별로 결과 표시 (예: "주문 3건 / 상품 1건 / SKU 2건 / 고객 1건 / 송장번호 1건"). 결과 클릭 → 해당 상세로 이동 + `recent_views` 자동 적재
- **최근 본 항목(🕐)**: 드롭다운으로 최근 조회한 주문/상품/고객 10건 표시(`recent_views` 최신순)
- **즐겨찾기(⭐)**: 드롭다운으로 즐겨찾기한 상품/보고서 표시, 각 화면(상품상세/보고서 화면)에는 별 아이콘 토글 배치
- **다크모드 토글(🌙)**: 라이트/다크 즉시 전환, 사용자 설정으로 저장(`system_settings` 또는 `users` 확장 컬럼에 `theme_preference` 저장)
- **AI Assistant 버튼(🤖)**: 상단바 우측 고정 아이콘. 클릭 시 우측 슬라이드 패널 오픈 (아래 8장 참고)

**참고**: 알림 벨(🔔)은 v1.0과 동일하게 유지, 배지 숫자는 `notifications.is_read=0` 기준.

---

## 2. 반응형 / 태블릿 레이아웃

| 화면 폭 | 레이아웃 동작 |
|---|---|
| 데스크톱 (≥1280px) | v1.0 기본 레이아웃 (좌측 사이드바 고정 + 콘텐츠) |
| 태블릿 (768~1279px) | 사이드바가 아이콘만 축소 표시(hover/tap 시 확장), 대시보드 그래프 2열→1열로 재배치, 테이블은 가로 스크롤 대신 핵심 컬럼만 우선 노출 + "더보기"로 상세 확장 |
| 모바일 대응(참고용, 본 프로젝트 1차 범위는 태블릿까지) | 사이드바는 하단 탭 또는 햄버거 메뉴로 전환 |

**설계 방침**: 화면 폭에 따라 컬럼 표시 우선순위를 화면별로 미리 정의한다 (예: 주문관리 테이블은 태블릿에서 "주문번호/상품/상태/금액"만 우선 노출, 나머지는 상세 클릭 시 확인).

---

## 3. 메인 대시보드 — 빠른 실행 버튼 추가

```
┌───────────────────────────────────────────────────────────────┐
│ 빠른실행: [▶ 주문수집] [▶ 광고수집] [▶ 보고서생성] [▶ 백업] [▶ 전체동기화]│
│           최근 실행: 주문수집 10:05 성공(23건) | 광고수집 09:00 성공  │
├───────────────────────────────────────────────────────────────┤
│  (v1.0의 KPI 카드/그래프/경고 스트립 동일, 전 항목 Drill-down 적용)│
└───────────────────────────────────────────────────────────────┘
```

**동작**
1. 버튼 클릭 → 확인 모달("쿠팡/네이버 등 전체 플랫폼 주문을 지금 수집할까요?") → 실행
2. `task_execution_history`에 RUNNING 레코드 생성, 버튼은 진행 중 스피너로 전환
3. 완료 시 토스트 알림("주문수집 완료: 신규 23건") + 버튼 하단에 최근 실행 결과 요약 갱신
4. 결과 클릭 → **작업 실행 이력** 화면으로 drill-down

---

## 4. 신규 화면 — 시스템 모니터링 (탭 3종 통합)

사이드바 "설정" 하위 또는 별도 "시스템 모니터링" 메뉴로 배치. 탭으로 3가지를 통합 관리한다.

```
┌───────────────────────────────────────────────────────────────┐
│ [연동상태] [작업이력] [시스템상태]                                 │
└───────────────────────────────────────────────────────────────┘
```

### 4.1 연동상태 (integration_status 기반)
```
┌───────────────────────────────────────────────────────────────┐
│ 구분   플랫폼        상태        마지막성공        토큰만료         │
│ 쇼핑몰 네이버스마트스토어 🟢정상    06-30 10:05      -                │
│ 쇼핑몰 쿠팡            🔴오류     06-29 22:00      -                │
│ 광고   네이버검색광고    🟡토큰만료임박 06-30 09:00   07-02 (D-2)     │
└───────────────────────────────────────────────────────────────┘
```
- 🔴 오류 클릭 → 최근 오류 메시지 + 관련 `task_execution_history` 목록으로 drill-down
- 🟡 토큰 만료임박 클릭 → 설정 > API Key관리로 이동

### 4.2 작업이력 (task_execution_history 기반)
```
┌───────────────────────────────────────────────────────────────┐
│ 필터: 유형[전체▾] 상태[전체▾] 기간[____~____]                     │
├───────────────────────────────────────────────────────────────┤
│ 시작시각   유형       트리거   상태    소요시간   결과                │
│ 10:05     주문수집    자동     성공    12초       신규 23건           │
│ 09:00     백업        자동     성공    45초       142MB               │
│ 08:50     광고수집    수동(관리자) 실패 8초        API 인증오류         │
└───────────────────────────────────────────────────────────────┘
```
- 실패 건 클릭 → 오류 상세 + 재시도 버튼

### 4.3 시스템상태
```
┌───────────────────────────────────────────────────────────────┐
│ DB 용량:  245MB / (모니터링 기준 없음, 추이 그래프)                │
│ 최근 백업: 06-30 03:00 성공 (142MB)  다음 예정: 07-01 03:00       │
│ 로그 용량: 이번달 38MB                                            │
│ 서버 상태: 정상 가동 (Uptime 12일)                                 │
└───────────────────────────────────────────────────────────────┘
```
- DB용량/로그용량은 실시간 조회(OS/DB 쿼리), 백업 상태는 `backup_history` 최신 레코드 기준

---

## 5. 상품관리 / 보고서 화면 — 즐겨찾기 아이콘 반영

- 상품관리 목록·상세: 각 행/상세 헤더에 ⭐ 토글 아이콘 → `favorites`(target_type=PRODUCT)
- 보고서 화면: 자주 쓰는 "유형+기간+필터" 조합을 ⭐로 저장 → 상단바 즐겨찾기 드롭다운에서 바로 재실행 가능 (예: "매주 월요일 확인하는 지난주 매출보고서")

---

## 6. 최근 본 항목 반영 규칙

주문 상세, 상품 상세, 고객 상세를 열람할 때마다 `recent_views`에 자동 기록한다. (조회수 통계 목적이 아니라 "방금 보던 걸 다시 찾기" 용도이므로 페이지 이동 시마다 매번 적재하지 않고, **동일 대상 재열람 시 viewed_at만 갱신**한다.)

---

## 7. Drill-down 일관성 규칙 (전 화면 공통 원칙)

v1.0에서는 화면별로 개별 서술했던 drill-down을, 아래처럼 **하나의 공통 규칙**으로 명문화하여 신규/기존 화면 모두에 일관 적용한다.

| 요소 유형 | 클릭 시 동작 |
|---|---|
| KPI 카드(오늘/이번달 등) | 해당 지표를 만드는 원본 목록 화면으로, 동일 기간 필터가 적용된 채로 이동 |
| 그래프의 데이터 포인트(막대/라인 클릭) | 해당 일자/구간·항목으로 필터링된 목록 화면으로 이동 |
| 통계 수치(반품률, ROAS 등 텍스트) | 해당 수치의 산출 근거가 되는 상세 목록으로 이동 |
| 경고 스트립(미배송/재고부족 등) | 해당 조건으로 필터링된 목록 화면으로 즉시 이동 |
| 알림(notifications) | 알림이 가리키는 대상(주문/정산/연동상태 등) 상세로 이동 |

**개발 지침**: 프론트엔드 공통 컴포넌트로 `<DrillLink metric="..." filters="...">`류의 래퍼를 만들어, 신규 화면 추가 시에도 이 규칙이 자동으로 지켜지도록 한다 (4단계 폴더 구조에서 공통 컴포넌트 위치 지정 예정).

---

## 8. ERP AI Assistant 버튼 (향후 AI 기능 연결 구조)

```
[🤖] 클릭 → 우측 슬라이드 패널
┌─────────────────────────────┐
│ ERP AI Assistant              │
│ ────────────────────────      │
│ "이번달 순이익이 지난달보다      │
│  낮은 이유를 분석해줘"          │
│ [____________________][전송]  │
│                                │
│ 빠른 질문:                     │
│  · 이번주 매출 요약             │
│  · 저ROAS 캠페인 찾기           │
│  · 반품 급증 원인               │
└─────────────────────────────┘
```

**설계 방침**
- 1차 개발 범위에서는 버튼과 패널 UI 뼈대만 배치하고, 실제 응답은 "준비 중" 안내로 대체하거나 `profit_loss_summary`/`ad_performance_daily` 등 기존 집계 테이블을 요약해 보여주는 수준의 간단한 로직으로 시작 가능
- 향후 `ai_analysis_results` 테이블과 연결되는 지점이 바로 이 패널이며, AI 모듈이 고도화되어도 **패널 UI는 그대로 두고 백엔드 응답 로직만 교체**하면 되도록 분리 설계
- 모든 화면에서 동일한 위치(상단바 우측)에 고정 노출하여, 사용자가 어떤 화면에서든 맥락 질문을 던질 수 있게 함(예: 손익분석 화면에서 열면 해당 화면 컨텍스트를 함께 전달하도록 향후 확장)

---

## 9. 변경 이력 요약

| 항목 | v1.0 | v1.1 |
|---|---|---|
| 상단바 | 로고/알림/사용자 | + 통합검색/최근본항목/즐겨찾기/다크모드/AI버튼 |
| 대시보드 | KPI+그래프+경고 | + 빠른실행 버튼 5종 |
| 사이드바 메뉴 | 15개 | + 시스템 모니터링(탭 3종) |
| DB 테이블 | 41개 | + recent_views, favorites, task_execution_history, integration_status → 45개 |
| Drill-down | 화면별 개별 서술 | 공통 규칙으로 통합, 전 화면 일관 적용 |
| 반응형 | 미정의 | 데스크톱/태블릿 기준 명시 |

---

## 10. 다음 단계 안내

1. SRS — 승인 완료
2. 데이터베이스 설계(ERD) — 승인 완료 (+ 본 문서로 4개 테이블 추가 확정)
3. UI 와이어프레임 v1.0 + **v1.1 추가분** — 승인 대기
4. 프로젝트 폴더 구조 설계 및 개발 착수

---

*본 문서는 검토 및 승인 후 수정될 수 있으며, 승인 시 다음 단계(폴더 구조 설계)로 진행합니다.*
